# Flask Configuration
FLASK_DEBUG=false
PORT=5000
HOST=0.0.0.0

# Model Configuration
MODEL_PATH=traffic-sign.h5
SIGNNAME_CSV=signname.csv

# Upload Configuration
UPLOAD_FOLDER=static/uploads
MAX_CONTENT_LENGTH=16777216

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE=logs/app.log

# CORS Configuration (set to true if accessing from different domains)
CORS_ENABLED=false
CORS_ORIGINS=*

# Model Loading (set to true for faster first prediction but slower startup)
EAGER_LOAD_MODEL=false
